<h1>Categories</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @categories.each do |category| %>
  <tr>
    <td><%= category.name %></td>
    <td><%= link_to 'Show', inventory_settings_category_path(category) %></td>
    <td><%= link_to 'Edit', edit_inventory_settings_category_path(category) %></td>
    <td><%= link_to 'Destroy', inventory_settings_category_path(category), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Category', new_inventory_settings_category_path %>
